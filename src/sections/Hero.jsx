import React from "react";
import { BackgroundPaths } from "@/components/ui/background-paths";

const Hero = () => {
  return (
    <section id="hero">
      <BackgroundPaths title="Alpenrose Digital Solutions" />
    </section>
  );
};

export default Hero;
